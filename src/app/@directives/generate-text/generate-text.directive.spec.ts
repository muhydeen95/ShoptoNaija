import { GenerateTextDirective } from './generate-text.directive';

describe('GenerateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateTextDirective();
    expect(directive).toBeTruthy();
  });
});
